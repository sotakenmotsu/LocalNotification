# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'LocalNotification' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LocalNotification
  pod ‘Firebase’
  pod ‘Firebase/Messaging’
  target 'LocalNotificationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LocalNotificationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
